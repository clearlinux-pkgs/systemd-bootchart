If the defaults in the config file are too short,
/run may not be mounted yet in time, it's better to just always
wait 1 second before writing out things.


--- systemd-bootchart-230/src/bootchart.c~	2016-06-08 00:39:34.000000000 +0000
+++ systemd-bootchart-230/src/bootchart.c	2016-06-08 12:12:35.647492267 +0000
@@ -27,7 +27,7 @@
   - Anders Norgaard - PyBootchartgui
   - Michael Meeks - bootchart2
   - Scott James Remnant - Ubuntu C-based logger
-  - Arjan van der Ven - for the idea to merge bootgraph.pl functionality
+  - Arjan van de Ven - for the idea to merge bootgraph.pl functionality
 
  ***/
 
@@ -463,6 +463,7 @@
                 t = time(NULL);
                 r = strftime(datestr, sizeof(datestr), "%Y%m%d-%H%M", localtime(&t));
                 assert_se(r > 0);
+                sleep(1);
 
                 snprintf(output_file, PATH_MAX, "%s/bootchart-%s.svg", arg_output_path, datestr);
                 of = fopen(output_file, "we");
